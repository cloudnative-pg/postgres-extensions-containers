apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: (join('-', [$values.extension_name, 'app']))
status:
  applied: true
  extensions:
  - applied: true
    name: ($values.extension_sql_name)
  observedGeneration: 1
