apiVersion: batch/v1
kind: Job
metadata:
  name: extension-installed
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: data-test
        env:
          - name: EXT_SQL_NAME
            value: ($values.extension_sql_name)
          - name: DB_URI
            valueFrom:
              secretKeyRef:
                name: (join('-', [$values.extension_name, 'app']))
                key: uri
        image: alpine/psql:latest
        command: ['sh', '-c']
        args:
         - |
           set -e
           DB_URI=$(echo $DB_URI | sed "s|/\*|/|")
           test "$(psql "$DB_URI" -tAc "SELECT EXISTS (SELECT FROM pg_catalog.pg_extension WHERE extname = '${EXT_SQL_NAME}')" -q)" = "t"
           echo "Extension '${EXT_SQL_NAME}' is installed!"
